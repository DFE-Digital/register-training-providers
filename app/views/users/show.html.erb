<%= render UserSummary::View.new(
      title: "View user", caption: "Support user", back_path: users_path,
      delete_path: @current_user == @user ? nil : user_delete_path(@user), editable: @current_user != @user, user: @user
    ) %>
