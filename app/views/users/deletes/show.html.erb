<%= content_for(:breadcrumbs) do %>
  <%= govuk_back_link(text: "Back", href: user_path(@user)) %>
<% end %>

<% page_data(header: "Confirm you want to delete #{@user.name}", title: "Confirm you want to delete user",
             caption: "Delete user") %>

<%= govuk_warning_text(text: "Deleting a user is permanent – you cannot undo it.") %>

<%= govuk_button_to("Delete user", user_delete_path(@user), method: :delete, warning: true) %>

<p class="govuk-body">
  <%= govuk_link_to("Cancel", user_path(@user)) %>
</p>
