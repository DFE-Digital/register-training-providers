<%= render CheckYourAnswers::View.new(
      rows: api_client_rows(model, change_path, method), title: "Change API client", caption: "API client - #{model.name}", back_path: back_path,
      save_button_text: "Save API client", save_path: save_path, cancel_path: api_clients_path, method: method
    ) %>
