<%= render CheckYourAnswers::View.new(
      rows: accreditation_rows(model, change_path),
      subtitle: method == :patch ? @provider.operating_name : "Add accreditation",
      caption: method == :patch ? @provider.operating_name : "Add accreditation",
      back_path: back_path,
      save_button_text: "Save accreditation",
      save_path: save_path,
      cancel_path: cancel_path,
      method: method
    ) %>
