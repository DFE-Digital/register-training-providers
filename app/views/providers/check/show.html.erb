<%= render CheckYourAnswers::View.new(
      rows: provider_rows(model, change_provider_type_path, change_path), subtitle: model.operating_name_was || model.operating_name, caption: model.operating_name_was || model.operating_name, back_path: back_path,
      save_button_text: "Save provider", save_path: save_path, cancel_path: providers_path, method: method
    ) %>
