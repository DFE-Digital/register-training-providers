<%= content_for(:breadcrumbs) do %>
  <%= render GovukComponent::BackLinkComponent.new(
        text: "Back",
        href: new_provider_contact_path(provider),
      ) %>
<% end %>

<%= render CheckYourAnswers::View.new(
      rows: contact_form_rows(model, change_path),
      subtitle: "Add contact - #{provider.operating_name}",
      caption: "Add contact - #{provider.operating_name}",
      back_path: back_path,
      save_button_text: "Save contact",
      save_path: save_path,
      cancel_path: cancel_path,
      method: method
    ) %>
