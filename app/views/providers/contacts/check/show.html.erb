<%= render CheckYourAnswers::View.new(
      rows: contact_form_rows(model, change_path),
      subtitle: method == :patch ? provider.operating_name : "Edit contact",
      caption: method == :patch ? provider.operating_name : "Edit contact",
      back_path: back_path,
      save_button_text: "Save contact",
      save_path: save_path,
      cancel_path: cancel_path,
      method: method
    ) %>
